# =======================================================
# CORE PLATFORM CONFIGURATION (Required for all services)
# =======================================================

# 1. DATABASE (PostgreSQL / TimescaleDB)
# Connection string for the shared relational database (Postgres + Prisma)
DATABASE_URL="postgresql://platform_user:platform_password@localhost:5432/platform_db"

# 2. CACHING & QUEUES (Redis / BullMQ)
# Connection string for the shared message broker and cache
REDIS_URL="redis://localhost:6379"

# 3. SECURITY (Authentication Service)
# Highly sensitive secret used by express-auth for JWT signing and verification.
JWT_SECRET="YOUR_HIGHLY_SECURE_32_CHAR_SECRET"


# =======================================================
# SERVICE-SPECIFIC CONFIGURATION
# =======================================================

# 4. AI SERVICE (Used by express-ai)
# API key required for LLM interaction (e.g., OpenAI, Anthropic, etc.)
LLM_PROVIDER_API_KEY="sk-******************************"

# 5. FRONTEND (Used by next-web)
# Public Next.js variables (prefixed NEXT_PUBLIC_)
NEXT_PUBLIC_API_URL="http://localhost:3001/api"
NEXT_PUBLIC_LLM_COST_WARNING="true"