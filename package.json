{
  "name": "platform-root",
  "private": true,
  "scripts": {
    "dev:all": "pnpm --parallel --filter \"./apps/*\" run dev",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "eslint": "^8.56.0",
    "prettier": "^3.2.5"
  }
}
