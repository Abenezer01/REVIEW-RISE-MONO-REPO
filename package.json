{
  "name": "platform-root",
  "private": true,
  "scripts": {
    "dev:all": "pnpm --parallel --filter \"./apps/*\" run dev",
    "install:db": "pnpm install @platform/db",
    "lint": "pnpm --recursive run lint",
    "test": "pnpm --recursive run test"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "eslint": "^8.56.0",
    "prettier": "^3.2.5"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
